on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      -
        name: Checkout
        uses: actions/checkout@v2
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      -
        name: Build and export
        uses: docker/build-push-action@v2
        with:
          context: .
          tags: ippsample:latest, ippsample:${{ github.sha }}
          outputs: type=docker,dest=/tmp/ippsample.tar
      -
        name: Upload artifact
        uses: actions/upload-artifact@v2
        with:
          name: ippsample
          path: /tmp/ippsample.tar

  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      -
        name: Download artifact
        uses: actions/download-artifact@v2
        with:
          name: ippsample
          path: /tmp
      -
        name: Load image
        run: |
          docker load --input /tmp/ippsample.tar
          docker image ls -a
      -
        name: Call commands
        run: |
          docker run ippsample:${{ github.sha }} ipp3dprinter --version
          docker run ippsample:${{ github.sha }} ippeveprinter --version
          docker run ippsample:${{ github.sha }} ippfind --version
          docker run ippsample:${{ github.sha }} ipptool --version
          docker run ippsample:${{ github.sha }} ippproxy --version -d ipp://1.example.com ipp://2.example.com
          docker run ippsample:${{ github.sha }} ippserver --version
          docker run ippsample:${{ github.sha }} ipptransform --version
          docker run ippsample:${{ github.sha }} ipptransform3d --version

  hub:
    runs-on: ubuntu-latest
    needs: test
    steps:
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      -
        name: Download artifact
        uses: actions/download-artifact@v2
        with:
          name: ippsample
          path: /tmp
      -
        name: Load image
        run: |
          docker load --input /tmp/ippsample.tar
          docker image ls -a
      -
        name: Tag image
        run: |
          docker tag ippsample:${{ github.sha }} ${{ secrets.DOCKERHUB_IMAGE }}:${{ github.sha }}
      -
        name: Login to DockerHub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      -
        name: Push image
        run: |
          docker push ${{ secrets.DOCKERHUB_IMAGE }}:${{ github.sha }}

  ghcr:
    runs-on: ubuntu-latest
    needs: test
    steps:
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      -
        name: Download artifact
        uses: actions/download-artifact@v2
        with:
          name: ippsample
          path: /tmp
      -
        name: Load image
        run: |
          docker load --input /tmp/ippsample.tar
          docker image ls -a
      -
        name: Tag image
        run: |
          docker tag ippsample:${{ github.sha }} istopwg/ippsample:${{ github.sha }}
      -
        name: Login to GitHub Container Registry
        uses: docker/login-action@v1 
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}
      -
        name: Push image
        run: |
          docker push ghcr.io/istopwg/ippsample:${{ github.sha }}
